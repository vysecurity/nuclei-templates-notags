id: rocketmq-console-exposure

info:
  name: Apache RocketMQ Console Exposure
  author: pdteam
  severity: info
 

requests:
  - method: GET
    path:
      - '{{BaseURL}}'

    matchers:
      - type: word
        words:
          - "<title>RocketMq-console-ng</title>"
