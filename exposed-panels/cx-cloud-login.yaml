id: cx-cloud-login

info:
  name: CX Cloud
  author: dhiyaneshDk
  severity: info
 

requests:
  - method: GET
    path:
      - '{{BaseURL}}'
      - '{{BaseURL}}/cxcum/'

    matchers:
      - type: word
        words:
          - "<title>CX Cloud</title>"
